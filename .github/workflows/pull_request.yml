name: Go

# Only trigger on pull requests to master
on:
  pull_request:
    types:
      - opened
      - reopened
    branches:
      - master

jobs:
  testing:
    runs-on: ubuntu-latest
    steps:
      - name: Debug
        run: echo "On pull request event"; env | sort

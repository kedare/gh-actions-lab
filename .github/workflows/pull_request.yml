name: Pull Request

# Only trigger on pull requests to master
on:
  pull_request:
    types:
      - opened
      - reopened
    branches:
      - master
      - main

jobs:
  testing:
    runs-on: ubuntu-latest
    steps:
      - name: Debug
        run: echo "On pull request event"; env | sort